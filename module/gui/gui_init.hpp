#ifndef GUI_INIT_HPP
#define GUI_INIT_HPP

/**
 * Initialize the GUI (LVGL + SDL).
 */
void gui_init(void);

/**
 * De-initialize the GUI (clean up SDL).
 */
void gui_deinit(void);

#endif // GUI_INIT_HPP
